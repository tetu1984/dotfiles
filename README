# init
git clone git@github.com:tetu1984/dotfiles.git ~/dotfiles
cd dotfiles
git submodule init
git submodule update


# vim
ln -s ~/dotfiles/_vimrc ~/.vimrc
ln -s ~/dotfiles/vimfiles ~/.vim

vim
:BundleInstall

# zsh
ln -s ~/dotfiles/_zshrc ~/.zshrc
